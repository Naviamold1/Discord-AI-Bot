[auth]
Token = YOUR_DISCORD_TOKEN

[trigger]
custom_trigger_word = None

[ai]
model = llama3
use_external_file_for_prompt = False    # If True, create a file named "prompt.txt" in the same directory as this config file with your desired prompt
prompt = Act as a real human. Don't acknowledge that you are an AI. Never question your existence. Limit your responses under 2000 characters.